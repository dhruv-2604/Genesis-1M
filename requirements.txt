# Core dependencies
numpy>=1.24.0
pyyaml>=6.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Optional: Ray for distributed execution (Phase 2+)
# ray[default]>=2.9.0

# Optional: vLLM for LLM inference (Phase 2+)
# vllm>=0.3.0

# Optional: LanceDB for vector memory (Phase 3+)
# lancedb>=0.3.0

# Optional: Visualization (future)
# matplotlib>=3.7.0
# plotly>=5.15.0
